{% extends "src/Vocabulaire/Resources/views/layout.html.twig" %}

{%set menu = 'homepage' %}
{% set route = 'vocabulaire' %}

{% block metaTitle %}Gestion Vocabulaire{% endblock %}

{% block content %}
    {{ parent() }}

    <div class="text-center">
        <h1>Gestion du vocabulaire nouveau</h1>
        <br /><br /><br />

        <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-3 list-group">
                <span class="list-group-item active">
                    <b>Données</b>
                </span>

                <span class="list-group-item text-left">
                    <span class="text-primary">Nombre de mots</span>
                    <span class="badge">{{ nbMot }}</span>
                </span>

                <span class="list-group-item text-left">
                    <span class="text-primary">Nombre de dictionnaires</span>
                    <span class="badge">{{ nbDictionnaire }}</span>
                </span>

                <span class="list-group-item text-left">
                    <span class="text-primary">Nb types de mots</span>
                    <span class="badge">{{ nbTypeMot }}</span>
                </span>
            </div>
        </div>
    </div>

{% endblock %}