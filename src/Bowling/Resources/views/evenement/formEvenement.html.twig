<div class="container-fluid">

    <div class=""row>
        <div class="form-group col-md-6 text-right">
            {{ form_row(form.code) }}<br />
            {{ form_row(form.typeJeu) }}
        </div>

        <div class="form-group col-md-4 text-right">
            {{ form_row(form.libelle) }}<br />
            {{ form_row(form.formation) }}
        </div>
    </div>

    <div class=""row>
        <div class="col-md-3 form form-inline text-right">
            {{ form_row(form.annuler) }}
            {{ form_row(form.save) }}
            <a href="{{ path('bowling-evenement') }}">{{ form_row(form.quitter) }}</a>
        </div>

    </div>

</div>